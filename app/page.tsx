export default function Home() {
  // Placeholder root page. The library grid (SPEC §6.1) is built in a later
  // ticket; this only proves the scaffold, theme and fonts are wired up.
  return (
    <main className="flex min-h-dvh items-center justify-center bg-bg text-text">
      <h1 className="font-sans text-2xl">Free Trace</h1>
    </main>
  );
}
