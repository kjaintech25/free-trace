# KNOWN_ISSUES.md

## iOS standalone-mode camera (SPEC §10.3)
Filled in by T-12. Must state the exact one-line change (file path + line) that makes the
Home Screen icon open the app in Safari instead of standalone mode, if the camera is black
after "Add to Home Screen". Do not implement it; document it.
